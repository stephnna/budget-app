<nav class="header">
  <ul class="navbar-nav me-auto mb-2 mb-lg-0">
    <li>
    <a href="/"><i class="fa fa-chevron-left"></i></a>
    </li>
    <li>
    <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "justify-content-center align-self-center btn btn-danger btn-sm sign-out" %>
      <h3>Transaction</h3>
    </li>
    <li>   
   </nav>
<div class="p-2 text-dark-50">
  <h3><%= @category.name.capitalize %>'s transactions</h3>
  <h4>Total spent: $<%= @category.total_payment %></h4>
</div>

  <% unless @payments.empty? %>
    <%= render 'list' %>
  <% else %>
    <p>No transaction available</p>
  <% end %>

<div class="w-100  py-1 px-1 cat-btn">
  <%= link_to "Add a transaction", new_category_payment_path(@category, @payment), :class => "btn btn-primary" %>
</div>
